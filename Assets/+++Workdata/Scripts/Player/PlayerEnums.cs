using UnityEngine;

public enum PlayerDirectionState{Left, Right, Down, Up}

public enum PlayerActionState {Default, Rolling, Jumping, Watering, Attacking, Fishing}

public class PlayerEnums : MonoBehaviour
{
    
}