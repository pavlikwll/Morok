using UnityEngine;

public enum PlayerDirectionState{Left, Right, Down, Up}
public enum PlayerActionState {Default, Rolling, Jumping, Attacking}
public enum PlayerMovementState{Idle, Walking}

public class PlayerEnums : MonoBehaviour
{
    
}